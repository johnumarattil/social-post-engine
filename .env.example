# AI caption generation (at least one required)
GEMINI_API_KEY=your-gemini-api-key-here
PERPLEXITY_API_KEY=pplx-xxxxxxxxxxxxxxxxxxxx

# Set to "false" to watch the browser during autopublish (debugging)
# Must be "true" (or unset) for the systemd service — there's no display.
# AUTOPUBLISH_HEADLESS=false
